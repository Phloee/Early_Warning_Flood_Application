import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF3B82F6);
  static const Color secondary = Color(0xFF10B981);
  static const Color background = Color(0xFFF8FAFC);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  
  // Status Colors (derived from specs implicitly)
  static const Color critical = Color(0xFFEF4444); // Red
  static const Color warning = Color(0xFFF59E0B); // Orange/Amber
  static const Color safe = Color(0xFF10B981); // Green, same as secondary
}
